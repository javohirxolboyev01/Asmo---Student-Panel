# Dev serverda /api so'rovlari vite.config.ts orqali http://localhost:4000 ga proksi qilinadi
# (backend CORS header qaytarmasa ham ishlaydi). Productionda backend boshqa domenda
# bo'lsa, to'liq URL qo'ying: VITE_API_URL=https://api.example.com/api
VITE_API_URL=/api
